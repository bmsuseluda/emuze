import { styled } from "~/stitches";
import { Box } from "../Box";

export const FormBox = styled(Box, {
  display: "flex",
  flexDirection: "column",
  gap: "$1",
  width: "400px",
  margin: 0,
  padding: 0,
});
